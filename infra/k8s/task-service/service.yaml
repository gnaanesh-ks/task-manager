apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: task-manager
  labels:
    app: task-service
spec:
  type: ClusterIP
  selector:
    app: task-service
  ports:
    - port: 4001
      targetPort: 4001
      name: http
