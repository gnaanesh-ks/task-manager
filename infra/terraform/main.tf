# This file intentionally left as an entry-point placeholder for module
# composition documentation. All resources are declared in vpc.tf and
# eks.tf so this project can be understood as a flat, single-environment
# root module. For multi-environment setups, wrap vpc.tf/eks.tf contents
# into a reusable module under modules/eks-cluster and call it from here
# per environment (dev/staging/prod) with distinct tfvars files.
